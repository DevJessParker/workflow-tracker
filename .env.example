# Workflow Tracker Environment Variables
# Copy this file to .env and fill in your values

# IMPORTANT: You MUST also create config/local.yaml from config/config.example.yaml
# The config file contains critical exclusions (node_modules, bin, obj, etc.)
# Without it, Docker will scan ALL files and take much longer!
#
# Steps:
# 1. cp config/config.example.yaml config/local.yaml
# 2. Edit config/local.yaml with your settings
# 3. cp .env.example .env
# 4. Edit .env with your values below

# Confluence Configuration
CONFLUENCE_URL=https://your-domain.atlassian.net
CONFLUENCE_USERNAME=your.email@company.com
CONFLUENCE_API_TOKEN=your-api-token-here
CONFLUENCE_SPACE_KEY=~YOURUSERID

# Repository to scan (for Docker)
# This should be the FULL PATH to your repository
REPO_TO_SCAN=/path/to/your/repo

# CI Mode (set to true for automated deployments)
CI_MODE=false
