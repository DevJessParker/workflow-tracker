# ============================================================================
# Workflow Tracker Environment Variables
# ============================================================================
# This is the SINGLE SOURCE OF TRUTH for credentials and paths!
#
# Quick Setup:
# 1. cp .env.example .env
# 2. Edit .env with your values below
# 3. cp config/config.example.yaml config/local.yaml
# 4. Run: docker-compose up workflow-tracker
#
# The config/local.yaml file references these variables using ${VAR_NAME} syntax
# This keeps credentials out of version control and in one place!
# ============================================================================

# ----------------------------------------------------------------------------
# Confluence Configuration (REQUIRED for publishing)
# ----------------------------------------------------------------------------
# Get API token from: https://id.atlassian.com/manage-profile/security/api-tokens
# Find your space key: Go to Confluence > Spaces > Personal (starts with ~)

CONFLUENCE_URL=https://your-domain.atlassian.net
CONFLUENCE_USERNAME=your.email@company.com
CONFLUENCE_API_TOKEN=your-api-token-here
CONFLUENCE_SPACE_KEY=~YOURUSERID

# ----------------------------------------------------------------------------
# Repository Configuration
# ----------------------------------------------------------------------------
# For Docker: Full path to your repository
# For local Python: Can be relative or absolute (defaults to current directory)
#
# Examples:
#   REPO_TO_SCAN=/home/user/projects/my-angular-app
#   REPO_TO_SCAN=C:\Users\jparker\source\MyApp
#   REPO_TO_SCAN=.  # Current directory (for local Python only)

REPO_TO_SCAN=/path/to/your/repo

# For Docker, this is set automatically to /repo
# For local Python, uncomment and set if needed:
# REPOSITORY_PATH=/path/to/your/repo

# ----------------------------------------------------------------------------
# CI/CD Configuration
# ----------------------------------------------------------------------------
# Set to true for automated deployments (TeamCity, Octopus Deploy, etc.)
# When true, runs headless and automatically publishes to Confluence

CI_MODE=false

# ----------------------------------------------------------------------------
# Optional: Override output directory
# ----------------------------------------------------------------------------
# OUTPUT_DIR=./output

# ----------------------------------------------------------------------------
# Optional: Auto-diagram modules/tables/endpoints
# ----------------------------------------------------------------------------
# Comma-separated lists for auto-diagram generation (used with --auto-diagrams flag)
# These can also be configured in config/local.yaml
#
# AUTO_DIAGRAM_MODULES=Services,Controllers
# AUTO_DIAGRAM_TABLES=Users,Orders,Products
# AUTO_DIAGRAM_ENDPOINTS=/api/users,/api/orders
